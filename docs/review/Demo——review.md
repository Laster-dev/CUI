# Demo —— 代码审查报告

- 审查日期：2026-09-19
- 工程路径：`Demo/`
- 规模：1 源文件（3 文件，含 vcxproj/filters）
- 方式：只读静态审查

---

## 1. 范围

最小可运行示例，用于验证 CUI.Core 的初始化与最小窗口创建流程。

---

## 2. 审查结论

体量极小，未发现 P0 级崩溃或安全问题。发现的问题集中在构建配置：

| 级别 | 问题 | 说明与建议 |
|---|---|---|
| P2 | **构建配置漂移**：`Demo.vcxproj` 仅在部分配置带 `/utf-8`（仓库已确认存在该漂移） | 全配置补 `/utf-8`；中文窗口标题/文案在非 `/utf-8` 配置下会乱码 |
| P2 | 工具集与其它工程混用（v143 / v145） | 统一写入 `Directory.Build.props` |
| P2 | 缺少最小错误处理示例（初始化失败如何提示） | 作为教学示例，建议补充框架初始化失败的分支与日志，起到示范作用 |

---

## 3. 优先修复建议

**P2（随手工地）**
1. 全配置补 `/utf-8`
2. 统一 `PlatformToolset`
3. 补充初始化失败的错误处理示范
