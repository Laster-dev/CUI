# Patcher —— 代码审查报告

- 审查日期：2026-09-19
- 工程路径：`Patcher/`
- 规模：5 源文件 / 4 头文件（12 文件）
- 方式：只读静态审查

---

## 1. 范围与架构

| 文件 | 职责 |
|---|---|
| `src/core/PePatcher.cpp` | PE 解析与补丁写入 |
| `src/core/PeSecurity.cpp` | 签名相关处理（剥离 / 校验） |
| 其余 `.cpp/.h` | CLI/UI 入口、文件读写、日志 |

---

## 2. P0 — 数据破坏

| # | 问题 | 位置 | 后果 | 建议 |
|---|---|---|---|---|
| 1 | `RemoveSignature` 条件写反 | `src/core/PeSecurity.cpp:106` | 正常文件被**截断**，原程序损坏且不可启动 | 修正判定条件；所有写操作前先做长度校验并生成备份 |
| 2 | 裸搜 `"level="` 做盲补丁 | `src/core/PeSecurity.cpp:151` | 命中数据区任意同名字节串 → 写入位置错误，文件被改坏 | 按 PE 结构定位目标字段；禁止字符串盲搜改写；补丁前校验前后字节上下文 |

---

## 3. P0 — 安全（越界解析）

| # | 问题 | 位置 | 后果 | 建议 |
|---|---|---|---|---|
| 3 | `InspectPe` 裸指针遍历节表/数据目录，无边界校验 | `src/core/PePatcher.cpp:582` | 畸形 PE 导致**越界读取/崩溃** | 每次读取前校验 `remaining >= need`；对 `NumberOfSections`、`SizeOfRawData`、`PointerToRawData` 做合理性检查 |
| 4 | 未校验 `IMAGE_DOS_SIGNATURE` / `IMAGE_NT_SIGNATURE` 与可选头魔数 | `PePatcher.cpp` | 非 PE 或 32/64 位混淆时按错误布局解析 | 依次校验 MZ / PE\0\0 / `IMAGE_NT_OPTIONAL_HDR_MAGIC` |

---

## 4. P1 — 正确性

| 问题 | 位置 | 说明与建议 |
|---|---|---|
| stub 尺寸硬编码 | `src/core/PePatcher.cpp:1056` | stub 变更后写入偏移错位，产出不可用文件 | 从资源/生成期导出实际尺寸常量，并加运行期断言 |
| 补丁后未更新校验和 | `PePatcher.cpp` | 部分加载器拒绝加载 | 可选调用 `CheckSumMappedFile` 重算 |
| 未处理已签名文件 | — | 修改后签名失效，行为不可预期 | 提示用户并提供"剥离签名"选项（配合备份） |

---

## 5. P1/P2 — 工程与安全加固

- **备份与回滚**：任何写操作前复制 `.bak`，失败自动回滚。
- **输入校验**：目标文件必须是 PE、非系统目录、非自身进程映像。
- **日志**：记录补丁偏移、旧值/新值，便于审计。
- **确认 `Patcher.vcxproj` 全配置带 `/utf-8`**（仓库存在配置漂移）。

---

## 6. 优先修复建议

**P0（立即，阻断发布）**
1. 修正 `RemoveSignature` 条件（#1）
2. `InspectPe` 全偏移边界校验（#3）
3. PE 头魔数校验（#4）
4. 移除盲搜补丁，改为结构化定位（#2）

**P1（本迭代）**
5. stub 尺寸改为生成期常量 + 断言（#5）
6. 写前备份 + 失败回滚
7. 可选重算校验和

**P2**
8. 补丁审计日志
9. 构建配置核对
